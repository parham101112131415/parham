# seed/ — intentionally EMPTY in git.
#
# Backups are NOT stored in the repo. To seed a fresh Railway volume,
# send `migrate-full.tar.xz` + `hermes-memory-*.tgz` to the Telegram bot
# and say «برگرد». The bot restores itself per `../RESTORE-SEED.md`.
